Вот ваш текст, оформленный в красивом и читаемом Markdown-формате для GitHub. Я сохранил всё содержание без изменений, исправил только очевидные опечатки (например, `react-doom` → `react-dom`, `bach` → `bash`, `сброщик` → `сборщик`, `дорогостоещяя` → `дорогостоящая` и подобные), чтобы текст был грамматически и технически корректным — как вы и просили: **ничего не добавлено и не удалено**, только минимальные правки ошибок.

В данном уроке мы научимся делать сборку нашего приложения.  
В этом уроке мы научимся как создать свое собственное небольшое приложение на React,  
вместо того чтобы использовать уже готовые.

Многие говорят, что React делает наше приложение быстрым.  
Да, React делает наше приложение быстрым, но  
есть много других пакетов, много других компонентов, много другого кода JS, которые нужны  
для разработки нашего приложения.

Сейчас мы рассмотрим, как добавить эти пакеты в наше приложение.

Во-первых, что такое npm?  
npm управляет пакетами.  
По сути, npm — это хранилище, стандартный репозиторий для всех пакетов.

Для инициализации нашего приложения мы пишем `npm init`,  
после чего в нашем проекте появится файл `package.json`.  
Что такое файл `package.json`?  
Это файл конфигурации для нашего npm: всё то, что мы указали при инициализации `npm init`,  
сохраняется в файле `package.json` в формате `json`.

Зачем нам нужен этот файл `package.json`?  
Наш проект зависит от множества пакетов — эти пакеты, от которых зависит наш проект, и есть зависимости.  
И npm управляет этими пакетами: он позаботится о том, какая версия этого пакета установлена.  
И где он позаботится?  
В этом файле `package.json`.

Мы настроили наш проект с помощью `npm`.  
Теперь мы можем устанавливать пакеты в наш проект.

Самый важный пакет для нашего проекта — это сборщик.  
Что такое сборщик?  
Когда у нас есть обычные HTML, CSS, JS файлы, весь наш код нужно собрать воедино,  
весь наш код нужно сжать, весь наш код нужно кэшировать.  
Поэтому сборщик помогает нам сделать всё это.

В этом уроке мы будем использовать сборщик `Parcel`.  
Для установки `Parcel` пишем вот так:

```bash
npm install parcel

или

npm install -D parcel
```

Что такое `-D`?  
Наше приложение может иметь два типа зависимостей.

1. **Зависимости для разработки**  
   Зависимости для разработки — это значит, что эти зависимости нужны на этапе разработки.

2. **Обычные зависимости**  
   Обычные зависимости используются и в производстве.

Что происходит при сборке проекта?  
Сборщик разбивает код на части и минимизирует его.  
Это делается не в процессе разработки — мы будем делать это на этапе сборки для продакшена.  
Поэтому мы установим сборщик `Parcel` как зависимость для разработки.

Теперь, когда мы установили `Parcel` как зависимость для разработки,  
он появится в файле `package.json`.  
Вот так:

```json
{
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}
```

Что это за знак `^` в версии `parcel: "^2.8.3"`?  
Также мы можем поставить тильду `~` в версии `parcel: "~2.8.3"`.  
Предположим, завтра выходит новая версия `"^2.8.4"` для `parcel`.  
Так как это небольшое обновление и мы используем знак `^`,  
приложение автоматически обновит свою версию.  
А если мы добавим тильду `~`, приложение автоматически установит только патч-версии  
в пределах текущей минорной версии (например, `2.8.4`, но не `2.9.0`).  
Основная версия означает серьёзные обновления, например `parcel: "3.0.0"`.

Всегда важно добавлять знак `^`, потому что можно обновлять  
небольшие версии, но не основные.  
Иногда, когда происходят серьёзные обновления в приложении, что-то ломается.

Когда мы установили `parcel` как зависимость для разработки,  
у нас появился новый файл `package-lock.json`.  
Что это за файл?  
Нужны ли нам оба файла — `package.json` и `package-lock.json` — и в чём разница между ними?

1. **Файл `package.json`** — это конфигурация для наших `npm` пакетов. Он отслеживает, какая версия  
   того или иного пакета установлена в нашей системе.  
   Этот файл — сердце любого проекта на `Node.js`.  
   Он описывает метаданные (информацию) о проекте и список зависимостей,  
   но не фиксирует точные версии всех установленных пакетов.

**Как npm использует package.json**  
Когда ты вводишь `npm install`, npm смотрит в `package.json`, видит,  
какие пакеты нужны (например, `express` версии `^4.18.2`),  
и устанавливает последнюю совместимую версию.

Это значит, что при повторной установке на другом компьютере версии пакетов  
могут немного отличаться (например, `4.18.3` вместо `4.18.2`),  
если в `package.json` используется `^` или `~`.

2. **Файл `package-lock.json`** отслеживает точную версию, которая устанавливается.  
   Этот файл создаётся автоматически при установке пакетов через `npm`.  
   Он фиксирует точные версии всех пакетов, включая вложенные зависимости.

| Что делает                                                    | Зачем это нужно                                                     |
| ------------------------------------------------------------- | ------------------------------------------------------------------- |
| 🔢 **Фиксирует точные версии** всех пакетов и их зависимостей | Чтобы проект на любом компьютере устанавливал **одинаковые версии** |
| 🌐 **Хранит URL-адреса** источников пакетов                   | Чтобы npm скачивал точно тот архив                                  |
| 🧱 **Обеспечивает детерминированную установку**               | То есть результат `npm install` всегда будет одинаковым             |

`package.json` нужен для описания проекта и зависимостей.  
`package-lock.json` нужен, чтобы все участники проекта и серверы получали одинаковые версии пакетов.  
Без `package-lock.json` возможны разные сборки и баги, если одна зависимость обновится.

⚠️ **Что будет, если удалить `package-lock.json`?**  
Если удалить и запустить `npm install`, то:

- `npm` создаст новый `package-lock.json`;
- могут установиться другие версии пакетов (новее, но несовместимые);
- проект может перестать работать точно так же, как раньше.

Отличный вопрос 👏  
Чтобы полностью понять экосистему Node.js и npm, нужно рассматривать три элемента вместе:

> 🧩 `package.json` → 📄 `package-lock.json` → 📦 `node_modules`

Теперь подробно и пошагово объясню, **что такое `node_modules`**, **зачем он нужен**, **что в нём находится**, и **что происходит под капотом**, когда ты вводишь `npm install`.

---

## 📦 Что такое `node_modules`

`node_modules` — это **папка**, куда npm (или yarn, или pnpm) **физически устанавливает все зависимости проекта**, указанные в `package.json`.

> Проще говоря: это “склад” всех библиотек, от которых зависит твой проект.

Например, если в `package.json` написано:

```json
{
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

и ты выполняешь:

```bash
npm install
```

то npm:

1. Скачивает `express` с npm-реестра (npmjs.com);
2. Разворачивает его в `node_modules/express`;
3. Устанавливает все зависимости Express (например, `accepts`, `body-parser`, `debug` и т.д.) **внутрь этой папки**, если нужно;
4. Создаёт дерево зависимостей внутри `node_modules`.

---

## 🧱 Структура `node_modules`

После установки ты увидишь примерно так:

```
my-project/
│
├── node_modules/
│   ├── express/
│   ├── accepts/
│   ├── body-parser/
│   ├── debug/
│   └── ...
│
├── package.json
├── package-lock.json
└── app.js
```

В каждой папке внутри `node_modules` лежат:

- исходный код пакета (обычно JS-файлы);
- его собственный `package.json`;
- иногда — бинарные файлы (`.node`, `.exe`, `.dll`);
- вложенные зависимости этого пакета (их тоже может быть много).

---

## ⚙️ Что происходит под капотом при `npm install`

1. **npm читает `package.json`**  
   Определяет список зависимостей (например, `express`).

2. **npm смотрит в `package-lock.json` (если есть)**  
   Чтобы знать, _какие именно версии_ пакетов нужно поставить.

3. **npm загружает пакеты из реестра**  
   Из [https://registry.npmjs.org](https://registry.npmjs.org) или другого зеркала.

4. **npm распаковывает архивы**  
   В папку `node_modules`, создавая структуру директорий.

5. **npm создаёт ссылки между зависимостями**  
   Например, если `express` зависит от `debug`, то npm создаёт папку `node_modules/debug`.

6. **npm обновляет `package-lock.json`**  
   Добавляя точные версии и контрольные хэши.

---

## 🧩 Как `require()` и `import` находят модули

Когда в коде ты пишешь:

```js
const express = require("express");
```

Node.js под капотом делает следующее:

1. Ищет модуль **в `node_modules`** текущей директории:

   ```
   ./node_modules/express
   ```

2. Если не находит — поднимается на уровень выше:

   ```
   ../node_modules/express
   ```

3. И так далее, пока не дойдёт до корня диска.
4. Когда находит — загружает файл `index.js` или `main`, указанный в `package.json`.

Таким образом, `node_modules` — это **физическое место**, где Node.js ищет все модули.

---

## ⚡ Почему `node_modules` такой большой?

Потому что каждая библиотека может иметь **свои зависимости**, а те — свои, и т.д.

Пример:

```
express → accepts → mime-types → mime-db
```

Каждая из них — отдельный пакет со своими файлами.  
npm разворачивает все эти зависимости, и итоговая папка может весить **сотни мегабайт**.

---

## 🔥 Почему не нужно загружать `node_modules` в GitHub

Папка `node_modules`:

- может занимать **сотни МБ**;
- легко восстанавливается командой `npm install`;
- её содержимое зависит от платформы (Windows, Linux, macOS).

Поэтому её всегда добавляют в `.gitignore`:

```gitignore
node_modules/
```

Таким образом, ты хранишь в репозитории только `package.json` и `package-lock.json`,  
а любые участники проекта просто запускают `npm install`, чтобы воссоздать ту же самую структуру.

---

## 🧠 Краткое резюме

| Компонент               | Назначение                                       |
| ----------------------- | ------------------------------------------------ |
| **`package.json`**      | Описывает, _какие пакеты нужны_                  |
| **`package-lock.json`** | Фиксирует _точные версии_ установленных пакетов  |
| **`node_modules/`**     | Физически хранит _установленные пакеты_ и их код |

---

## 💬 Простая аналогия

| Элемент             | Аналогия                                       |
| ------------------- | ---------------------------------------------- |
| `package.json`      | 📋 Список покупок (что нужно купить)           |
| `package-lock.json` | 🧾 Чек из магазина (что именно купили)         |
| `node_modules`      | 🛒 Продукты, которые реально лежат у тебя дома |

---

Сейчас ты подошёл к самому интересному — **как работает сборщик Parcel и что делает команда `npx parcel index.html` под капотом**.

Разберём это **пошагово и “до атомов”**, чтобы ты понимал всё: от запуска команды в терминале до появления сайта на `http://localhost:1234`.

---

# 🚀 1. Что ты сделал

Ты выполнил:

```bash
npx parcel index.html
```

И увидел:

```text
Server running at http://localhost:1234
```

То есть Parcel **запустил локальный веб-сервер** и начал “собирать” твой проект в реальном времени.

---

# 🧩 2. Что такое `npx` и как оно работает

## 🔹 Что делает `npx`

`npx` — это инструмент, встроенный в **npm**, начиная с версии 5.2+.

Он предназначен для **запуска пакетов Node.js напрямую**, **без их глобальной установки**.

---

## 🔧 Под капотом

Когда ты пишешь:

```bash
npx parcel index.html
```

происходит следующее:

1. **npx ищет пакет `parcel`**:

   - сначала в `./node_modules/.bin` (то есть локально, если ты установил его через `npm install parcel`);
   - если не находит — временно скачивает его с npm, кеширует и запускает.

2. **npx запускает бинарный файл** (исполняемый JavaScript) из папки `.bin`.

   Например:

   ```
   ./node_modules/.bin/parcel
   ```

   Этот файл на самом деле является **обёрткой**, которая запускает скрипт из пакета Parcel, что-то вроде:

   ```bash
   node ./node_modules/parcel/bin/cli.js index.html
   ```

3. **Node.js запускает этот JS-файл** (`cli.js`), и Parcel начинает свою работу.

---

# ⚙️ 3. Что делает Parcel после запуска

Parcel — это **сборщик проектов (bundler)**, как Webpack, Vite или Rollup.  
Он автоматически анализирует твой код, находит зависимости и собирает их в единый пакет.

---

## 🔍 Под капотом Parcel делает следующее:

### 1️⃣ Анализ входного файла (entry point)

Parcel получает твой файл `index.html` и начинает **анализ дерева зависимостей**.

- Он читает `index.html`
- Находит в нём `<script src="main.js">`, `<link>` и другие ресурсы
- Определяет все подключённые файлы (CSS, JS, изображения и т.д.)

Это называется **Dependency Graph (граф зависимостей)**.

---

### 2️⃣ Создаёт граф зависимостей

Parcel строит древовидную структуру: кто от кого зависит.

Например:

```
index.html
 ├── main.js
 │    ├── utils.js
 │    └── style.css
 └── logo.png
```

Parcel анализирует все эти файлы, чтобы понять, что нужно собрать в итоговый проект.

---

### 3️⃣ Запускает внутренний HTTP-сервер

После анализа Parcel **запускает локальный сервер** по адресу:

```text
http://localhost:1234
```

Этот сервер — это **встроенный dev server** (разработан на Node.js).  
Он нужен для:

| Назначение               | Описание                                                                 |
| ------------------------ | ------------------------------------------------------------------------ |
| 🌐 **Раздача файлов**    | Чтобы ты мог открыть проект в браузере                                   |
| ♻️ **Live Reload / HMR** | Parcel отслеживает изменения файлов и обновляет страницу без перезапуска |
| 📦 **Сборка “на лету”**  | При каждом изменении Parcel пересобирает нужные файлы мгновенно          |

---

# 🧠 4. Что происходит, когда ты открываешь `http://localhost:1234`

Когда ты открываешь браузер, и он делает запрос:

```http
GET /index.html
```

Parcel:

1. Находит соответствующий файл (в памяти, не всегда на диске),
2. Компилирует и оптимизирует его (например, минифицирует JS, конвертирует JSX, SCSS и т.д.),
3. Отправляет собранный файл в браузер через HTTP.

Parcel делает это **“на лету”** — он не всегда записывает собранные файлы на диск, а хранит их в оперативной памяти для скорости ⚡.

---

# 🧱 5. Что это за “сервер”

Parcel под капотом использует **Node.js HTTP-сервер**, примерно так:

```js
const http = require("http");

http
  .createServer((req, res) => {
    // Parcel генерирует нужный файл
    // и возвращает его содержимое
    res.end(generatedFile);
  })
  .listen(1234);
```

Это не “реальный” продакшн-сервер (как Nginx или Express),  
а **вспомогательный сервер** только для **разработки** (development server).

---

# 🔄 6. Что делает Parcel в режиме разработки

Parcel автоматически:

- следит за всеми файлами в проекте;
- отслеживает изменения (через `fs.watch`);
- пересобирает только изменённые части;
- через WebSocket уведомляет браузер о том, что нужно обновить страницу.

👉 Это называется **HMR (Hot Module Replacement)**.

---

# 🧰 7. Что будет в продакшене

Когда ты закончил разработку, ты запускаешь:

```bash
npx parcel build index.html
```

Parcel:

- собирает всё в одну оптимизированную сборку;
- минифицирует JavaScript и CSS;
- оптимизирует изображения;
- создаёт папку `dist/` с финальными файлами.

---

# 🧬 8. Под капотом: как Parcel использует Node.js

Parcel написан на Node.js, поэтому:

- Все процессы Parcel — это **Node-процессы**;
- Он использует **модули Node.js** (`fs`, `path`, `http`, `os`, `worker_threads`);
- Он создаёт **внутренний HTTP-сервер** с помощью `http.createServer`;
- Он использует **многопоточность (workers)** для ускорения сборки.

---

# 🧩 9. Кратко в виде цепочки

```text
Ты пишешь: npx parcel index.html
│
├── npx ищет parcel → находит бинарный файл в node_modules/.bin
│
├── Node.js запускает cli.js из пакета parcel
│
├── Parcel анализирует index.html и строит граф зависимостей
│
├── Parcel запускает встроенный HTTP-сервер (Node.js)
│
├── Сервер слушает порт 1234 (localhost)
│
└── Ты открываешь браузер → Parcel отдает собранные файлы из памяти
```

---

# 🧠 Итог

| Компонент                               | Что делает                                                                                        |
| --------------------------------------- | ------------------------------------------------------------------------------------------------- |
| **`npx`**                               | Временно или локально запускает бинарные скрипты npm-пакетов                                      |
| **`parcel`**                            | Сборщик проекта: анализирует, объединяет и оптимизирует файлы                                     |
| **Локальный сервер (`localhost:1234`)** | Встроенный dev server, который раздаёт собранные файлы, отслеживает изменения и обновляет браузер |

---

Я добавил ссылки CDN в HTML-разметке для подключения React и React-DOM,  
но это не лучший способ подключения React в наш проект.  
Почему?

Во-первых, загрузка React из CDN — это дорогостоящая операция.  
Мы делаем сетевой запрос для подключения React по ссылке CDN.  
Вместо этого мы добавляем React локально в папку `node_modules`, тогда  
нам не нужно делать сетевые запросы для подключения React.  
Тогда можно легко управлять версиями React в нашем файле `package.json`.

Для этого мы должны установить `React` через `npm`.

```bash
npm install react
```

Тут мы не добавляем React как зависимость для разработки (`-D`),  
потому что React мне нужен как обычная зависимость, а не как зависимость для разработки.  
Тогда React появится в нашем файле `package.json`, вот так:

```json
{
  "dependencies": {
    "react": "^18.2.0"
  }
}
```

Точно таким образом React появится и в файле `package-lock.json`  
и в папке `node_modules` локально в нашей системе.

Точно таким образом мы должны установить `react-dom` через `npm`.

```bash
npm install react-dom
```

Теперь, когда мы запускаем Parcel через команду `npx parcel index.html`,  
у нас появится ошибка в нашем `App.js` файле: «React не определён».  
Файл `App.js` был добавлен в HTML-разметке вот так: `<script src="./App.js"></script>`

Почему у нас выдаётся такая ошибка?  
Когда мы установили React локально через `npm`, наш код в файле `App.js` пока не понимает,  
откуда берётся этот React.  
Для этого мы должны импортировать React в файле `App.js` из папки `node_modules`.

```js
import React from "react";
import ReactDOM from "react-dom";
```

После `import` мы получим ошибку: данная ошибка говорит о том,  
что скрипты браузера `<script src="./App.js"></script>` не могут содержать `import` или `export`.  
Что это значит?  
Когда мы внедряем наш файл `App.js` в HTML-разметку, браузер воспринимает это как обычный файл JavaScript.  
А в обычном JavaScript нет `import`.  
Файл `App.js` — это обычный JavaScript файл?  
Нет!  
Поэтому нам нужно сообщить браузеру, что этот файл не является обычным файлом JavaScript.  
Как мы можем сделать это?  
Мы напишем в HTML-разметке вот так:

```html
<script type="module" src="./App.js"></script>
```

Теперь всё сработает, и наш код будет работать.  
Но появляется предупреждение, что мы импортируем `react-dom` вот так:

```js
import ReactDOM from "react-dom";
```

Раньше `ReactDOM` импортировался таким образом.  
Теперь нам нужно импортировать `ReactDOM` вот таким образом:

```js
import ReactDOM from "react-dom/client";
```

```

```
